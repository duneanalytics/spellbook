version: 2

seeds:
  - name: nomad_ethereum_transactions_etherscan
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        block_time: timestamp
        original_amount_raw: uint256