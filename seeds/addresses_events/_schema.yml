version: 2

seeds:
  - name: first_funded_by_seed
    config:
      tags: dunesql
      column_types:
        blockchain: varchar
        address: varbinary
        first_funded_by: varbinary
        block_number: uint256
        tx_hash: varbinary