version: 2

seeds:
  - name: looksrare_ethereum_v1_trades
    config:
      column_types:
        amount_raw: decimal(38)
        platform_fee_amount_raw: decimal(38)
        royalty_fee_amount_raw: decimal(38)

  - name: looksrare_ethereum_v2_trades
    config:
      column_types:
        amount_raw: decimal(38)
        platform_fee_amount_raw: decimal(38)
        royalty_fee_amount_raw: decimal(38)
