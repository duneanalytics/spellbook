version: 2

seeds:
  - name: xchange_ethereum_trades_seed
    config:
      tags: dunesql
      column_types:
        blockchain: varchar
        project: varchar
        version: varchar
        block_date: date
        tx_hash: varbinary
        evt_index: integer
        token_bought_address: varbinary
        token_bought_amount: double
        token_sold_address: varbinary
        token_sold_amount: double

