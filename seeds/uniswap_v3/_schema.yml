version: 2

seeds:
  - name: uniswap_v3_proposals_seed
    config:
      tags: dunesql
      column_types:
        blockchain: varchar
        proposal_id: int
        outcome: varchar

  - name: uniswap_v3_votes_seed
    config:
      tags: dunesql
      column_types:
        blockchain: varchar
        proposal_id: int
        voter_address: varbinary
        votes: bigint
