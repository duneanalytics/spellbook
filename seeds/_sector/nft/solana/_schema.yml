version: 2

seeds:
  - name: magiceden_solana_trades_seed
    config:
      tags: dunesql
      column_types:
        block_slot: bigint
        tx_id: varchar
        buyer: varchar
        seller: varchar
        outer_instruction_index: int
        inner_instruction_index: int
        account_mint: varchar
        amount_raw: double
        taker_fee_amount_raw: double
        maker_fee_amount_raw: double
        royalty_fee_amount_raw: double

  - name: tensorswap_solana_trades_seed
    config:
      tags: dunesql
      column_types:
        block_slot: bigint
        tx_id: varchar
        buyer: varchar
        seller: varchar
        outer_instruction_index: int
        inner_instruction_index: int
        account_mint: varchar
        amount_raw: double
        taker_fee_amount_raw: double
        maker_fee_amount_raw: double
        royalty_fee_amount_raw: double
