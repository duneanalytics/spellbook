version: 2

seeds:
  - name: archipelago_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: foundation_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: superrare_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: blur_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: blur_seaport_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: blur_v2_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: element_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: x2y2_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: zora_v1_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: zora_v2_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: zora_v3_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: cryptopunks_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: looksrare_v1_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: looksrare_v2_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: looksrare_seaport_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: collectionswap_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: sudoswap_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  # solana seeds below here
  - name: magiceden_solana_base_trades_seed
    config:
      tags: dunesql
      column_types:
        block_slot: uint256
        tx_id: varchar
        buyer: varchar
        seller: varchar
        outer_instruction_index: uint256
        inner_instruction_index: uint256
        account_mint: varchar
        amount_raw: uint256
        taker_fee_amount_raw: uint256
        maker_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256

  - name: tensorswap_solana_base_trades_seed
    config:
      tags: dunesql
      column_types:
        block_slot: uint256
        tx_id: varchar
        buyer: varchar
        seller: varchar
        outer_instruction_index: uint256
        inner_instruction_index: uint256
        account_mint: varchar
        amount_raw: uint256
        taker_fee_amount_raw: uint256
        maker_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256