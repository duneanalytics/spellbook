version: 2

seeds:
  - name: archipelago_ethereum_base_trades_seed
    config:
      column_types:
        price_raw: decimal(38)
        platform_fee_amount_raw: decimal(38)
        royalty_fee_amount_raw: decimal(38)

  - name: foundation_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        buyer: varbinary
        seller: varbinary
        nft_contract_address: varbinary
        price_raw: uint256
        platform_fee_amount_raw: uint256
        royalty_fee_amount_raw: uint256
        nft_token_id: uint256

  - name: superrare_ethereum_base_trades_seed
    config:
      column_types:
        price_raw: decimal(38)
        platform_fee_amount_raw: decimal(38)
        royalty_fee_amount_raw: decimal(38)

  - name: blur_ethereum_base_trades_seed
    config:
      column_types:
        price_raw: decimal(38)
        platform_fee_amount_raw: decimal(38)
        royalty_fee_amount_raw: decimal(38)

  - name: element_ethereum_base_trades_seed
    config:
      column_types:
        price_raw: decimal(38)
        platform_fee_amount_raw: decimal(38)
        royalty_fee_amount_raw: decimal(38)

  - name: x2y2_ethereum_base_trades_seed
    config:
      column_types:
        price_raw: decimal(38)
        platform_fee_amount_raw: decimal(38)
        royalty_fee_amount_raw: decimal(38)

  - name: zora_v1_ethereum_base_trades_seed
    config:
      column_types:
        price_raw: decimal(38)
        platform_fee_amount_raw: decimal(38)
        royalty_fee_amount_raw: decimal(38)

  - name: zora_v2_ethereum_base_trades_seed
    config:
      column_types:
        price_raw: decimal(38)
        platform_fee_amount_raw: decimal(38)
        royalty_fee_amount_raw: decimal(38)

  - name: zora_v3_ethereum_base_trades_seed
    config:
      column_types:
        price_raw: decimal(38)
        platform_fee_amount_raw: decimal(38)
        royalty_fee_amount_raw: decimal(38)

  - name: cryptopunks_ethereum_base_trades_seed
    config:
      column_types:
        price_raw: decimal(38)
        platform_fee_amount_raw: decimal(38)
        royalty_fee_amount_raw: decimal(38)

  - name: looksrare_v1_ethereum_base_trades_seed
    config:
      column_types:
        price_raw: decimal(38)
        platform_fee_amount_raw: decimal(38)
        royalty_fee_amount_raw: decimal(38)
        nft_token_id: string

  - name: looksrare_v2_ethereum_base_trades_seed
    config:
      column_types:
        price_raw: decimal(38)
        platform_fee_amount_raw: decimal(38)
        royalty_fee_amount_raw: decimal(38)
        nft_token_id: string

