version: 2

seeds:
  - name: decentraland_polygon_sample_trades
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        nft_contract_address: varbinary
        token_id: uint256
        buyer: varbinary
        seller: varbinary
