version: 2
seeds:
  - name: aave_arbitrum_base_borrow_seed
    config:
      column_types:
        blockchain: varchar
        project: varchar
        version: varchar
        block_number: bigint
        block_time: timestamp
        tx_hash: varbinary
        evt_index: bigint
        transaction_type: varchar
        token_address: varbinary
        borrower: varbinary
        amount: double
