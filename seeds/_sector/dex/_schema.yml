version: 2

seeds:
  - name: dex_base_trades_seed
    config:
      column_types:
        blockchain: varchar
        version: varchar
        tx_hash: varbinary
        evt_index: uint256
        block_number: uint256
        token_bought_address: varbinary
        token_sold_address: varbinary
        token_bought_amount_raw: uint256
        token_sold_amount_raw: uint256
        block_date: timestamp

  - name: defiswap_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        version: varchar
        tx_hash: varbinary
        token_bought_address: varbinary
        token_sold_address: varbinary
        token_bought_amount_raw: uint256
        token_sold_amount_raw: uint256

  - name: uniswap_ethereum_base_trades_seed
    config:
      tags: dunesql
      column_types:
        version: varchar
        tx_hash: varbinary
        token_bought_address: varbinary
        token_sold_address: varbinary
        token_bought_amount_raw: uint256
        token_sold_amount_raw: uint256

  - name: uniswap_celo_base_trades_seed
    config:
      tags: dunesql
      column_types:
        version: varchar
        tx_hash: varbinary
        token_bought_address: varbinary
        token_sold_address: varbinary
        token_bought_amount_raw: uint256
        token_sold_amount_raw: uint256

  - name: mento_celo_base_trades_seed
    config:
      tags: dunesql
      column_types:
        version: varchar
        tx_hash: varbinary
        token_bought_address: varbinary
        token_sold_address: varbinary
        token_bought_amount_raw: uint256
        token_sold_amount_raw: uint256
