version: 2

seeds:
  - name: balances_base_erc20_latest_entries
    description: sample entries
    config:
      column_types:
        amount_raw: double
        wallet_address: varbinary
        token_address: varbinary

  - name: balances_base_erc20_specific_wallet
    description: sample entries
    config:
      column_types:
        amount_raw: double
        wallet_address: varbinary
        token_address: varbinary

  - name: balances_base_erc20_daily_entries
    description: sample entries
    config:
      column_types:
        amount_raw: double
        wallet_address: varbinary
        token_address: varbinary