version: 2

seeds:
  - name: zeroex_ethereum_api_fills_sample
    config:
      tags: dunesql
      column_types:
        tx_hash: varbinary
        block_number: bigint
        evt_index: int
        contract_address: varbinary
        block_time: timestamp
        maker: varbinary
        taker: varbinary
        taker_token: varbinary
        taker_symbol: varchar
        maker_token: varbinary
        maker_symbol: varchar
        token_pair: varchar
        taker_token_amount: double
        taker_token_amount_raw: varchar
        maker_token_amount: double
        maker_token_amount_raw: varchar
        type: varchar
        affiliate_address: varbinary
        swap_flag: boolean
        matcha_limit_order_flag: boolean
        volume_usd: double
        tx_from: varbinary
        tx_to: varbinary
  - name: zeroex_ethereum_native_fills_sample
    config:
      tags: dunesql
      column_types:
        blockchain: varchar
        block_date: timestamp
        block_time: timestamp
        taker_symbol: varbinary
        maker_symbol: varbinary
        token_pair: varchar
        taker_token_amount: double
        maker_token_amount: double
        taker_token_amount_raw: varchar 
        maker_token_amount_raw: varchar
        volume_usd: double
        taker_token: varbinary
        maker_token: varbinary
        taker: varbinary
        maker: varbinary
        affiliate_address: varbinary
        tx_hash: varbinary
        tx_from: varbinary
        tx_to: varbinary
        evt_index: int
        type: varchar
        swap_flag: boolean
        fills_within: int