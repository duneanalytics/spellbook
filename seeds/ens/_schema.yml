version: 2

seeds:
  - name: ens_view_registrations_postgres
    config:
      tags: dunesql
      column_types:
        label: varbinary
        evt_block_number: bigint
        evt_index: bigint
  - name: ens_view_registries_postgres
    config:
      tags: dunesql
      column_types:
        label: varbinary
        min_evt_block_time: timestamp
  - name: ens_view_renewals_postgres
    config:
      tags: dunesql
      column_types:
        label: varbinary
        evt_block_number: bigint
        evt_index: bigint