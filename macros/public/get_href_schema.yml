version: 2

macros:
  - name: get_href_schema
    description: "Converts a link and text to the clickable href link"
    arguments:
      - name: link
        type: string
        description: "link you want to point new tab to"
      - name: text
        type: string
        description: "text you want to display"
