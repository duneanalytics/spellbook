version: 2

macros:
  - name: get_chain_native_token
    description: "gets either the symbol, prices_symbol, or prices_address"
    arguments:
      - name: chain
        type: a valid chain in dune
        description: "any of the v2 engine chain names, so polygon, arbitrum, avalanche_c, ethereum, gnosis, bnb, optimism, solana..."
      - name: column
        type: string
        description: "gets either the symbol, prices_symbol, or prices_address"
