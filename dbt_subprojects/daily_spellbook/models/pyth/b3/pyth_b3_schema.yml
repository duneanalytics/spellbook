version: 2

models: 
  - name: pyth_entropy_b3_request
    meta:
      blockchain: b3
      project: pyth
    config:
      tags: ['b3', 'pyth', 'entropy', 'request', 'vrf']
    description: "Pyth Entropy request on B3"
    columns:
      - name: blockchain
      - name: tx_hash
        tests:
          - unique
          - not_null
      - name: fee
        tests:
          - not_null
      - name: symbol
      - name: provider
        tests:
          - not_null
      - name: block_time
      - name: block_date
      - name: caller