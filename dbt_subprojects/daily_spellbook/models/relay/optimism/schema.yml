version: 2

models:
  - name: relay_token_transfers_optimism
    description: "Token transfers from the Relay address on Optimism. Includes ETH and ERC-20 transfers."
    meta:
      blockchain: optimism
      project: relay
      contributors: outputlayer
    config:
      tags: ['relay', 'token', 'transfers']
    columns:
      - name: evt_tx_hash
        tests: [not_null]
      - name: evt_block_time
        tests: [not_null]
      - name: to
      - name: symbol
      - name: value
