version: 2

models:
  - name: dns_ton_wallet_updates
    meta:
      blockchain: ton
      sector: infrastructure
      contributors: markysha
    config:
      tags: ['ton', 'dns', 'infrastructure']
    description: >
      TON Domains wallets info
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - block_time
            - tx_hash
            - domain
    columns:
      - name: block_time
        description: "block_time of the transaction"
      - name: tx_hash
        description: "transaction hash"
      - name: trace_id
        description: "trace id"
      - name: domain
        description: ".ton domain"
      - name: nft_item_address
        description: "address of .ton domain nft item"
      - name: wallet
        description: "address to which .ton domain reffers"
  
