version: 2

models:
  - name: addresses_ethereum_first_funding
    meta:
      blockchain: ethereum
      sector: addresses
      contributors: kryptaki
    config:
      tags: ['address', 'first_funding']
    description: "Per-address first funding for Ethereum (first funded, contract flag). Use _updated_at for incremental reads."
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address
    columns:
      - &address
        name: address
        description: "Address"
      - &blockchain
        name: blockchain
        description: "Blockchain"
      - &chain_id
        name: chain_id
        description: "Chain ID from blockchains source"
      - &first_funded_by
        name: first_funded_by
        description: "Address that first funded this address with this chain's native token"
      - &first_funded_at
        name: first_funded_at
        description: "Block time at which the address was first funded with this chain's native token"
      - &is_smart_contract
        name: is_smart_contract
        description: "True if the address is a smart contract"
      - &is_eoa
        name: is_eoa
        description: "True if the address is an externally owned account (EOA)"
      - &_updated_at
        name: _updated_at
        description: "When this row was last written; use for incremental sync (e.g. WHERE _updated_at > :last_sync)"

  - name: addresses_arbitrum_first_funding
    meta:
      blockchain: arbitrum
      sector: addresses
      contributors: kryptaki
    config:
      tags: ['address', 'first_funding']
    description: "Per-address first funding for Arbitrum"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address
    columns:
      - *address
      - *blockchain
      - *chain_id
      - *first_funded_by
      - *first_funded_at
      - *is_smart_contract
      - *is_eoa
      - *_updated_at

  - name: addresses_avalanche_c_first_funding
    meta:
      blockchain: avalanche_c
      sector: addresses 
      contributors: kryptaki
    config:
      tags: ['address', 'first_funding']
    description: "Per-address first funding for Avalanche"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address
    columns:
      - *address
      - *blockchain
      - *chain_id
      - *first_funded_by
      - *first_funded_at
      - *is_smart_contract
      - *is_eoa
      - *_updated_at

  - name: addresses_base_first_funding
    meta:
      blockchain: base
      sector: addresses   
      contributors: kryptaki
    config:
      tags: ['address', 'first_funding']
    description: "Per-address first funding for Base"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address
    columns:
      - *address
      - *blockchain
      - *chain_id
      - *first_funded_by
      - *first_funded_at
      - *is_smart_contract
      - *is_eoa
      - *_updated_at

  - name: addresses_bnb_first_funding
    meta:
      blockchain: bnb
      sector: addresses 
      contributors: kryptaki
    config:
      tags: ['address', 'first_funding']
    description: "Per-address first funding for BNB"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address
    columns:
      - *address
      - *blockchain
      - *chain_id
      - *first_funded_by
      - *first_funded_at
      - *is_smart_contract
      - *is_eoa
      - *_updated_at

  - name: addresses_linea_first_funding
    meta:
      blockchain: linea
      sector: addresses   
      contributors: kryptaki  
    config:
      tags: ['address', 'first_funding']
    description: "Per-address first funding for Linea"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address
    columns:
      - *address
      - *blockchain
      - *chain_id
      - *first_funded_by
      - *first_funded_at
      - *is_smart_contract
      - *is_eoa
      - *_updated_at

  - name: addresses_optimism_first_funding
    meta:
      blockchain: optimism
      sector: addresses   
      contributors: kryptaki
    config:
      tags: ['address', 'first_funding']
    description: "Per-address first funding for Optimism"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address
    columns:
      - *address
      - *blockchain
      - *chain_id
      - *first_funded_by
      - *first_funded_at
      - *is_smart_contract
      - *is_eoa
      - *_updated_at

  - name: addresses_polygon_first_funding
    meta:
      blockchain: polygon
      sector: addresses   
      contributors: kryptaki
    config:
      tags: ['address', 'first_funding']
    description: "Per-address first funding for Polygon"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address
    columns:
      - *address
      - *blockchain
      - *chain_id
      - *first_funded_by
      - *first_funded_at
      - *is_smart_contract
      - *is_eoa
      - *_updated_at

  - name: addresses_zksync_first_funding
    meta:
      blockchain: zksync
      sector: addresses   
      contributors: kryptaki
    config:
      tags: ['address', 'first_funding']
    description: "Per-address first funding for zkSync"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address
    columns:
      - *address
      - *blockchain
      - *chain_id
      - *first_funded_by
      - *first_funded_at
      - *is_smart_contract
      - *is_eoa
      - *_updated_at

  - name: addresses_monad_first_funding
    meta:
      blockchain: monad
      sector: addresses
      contributors: kryptaki
    config:
      tags: ['address', 'first_funding']
    description: "Per-address first funding for Monad"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address
    columns:
      - *address
      - *blockchain
      - *chain_id
      - *first_funded_by
      - *first_funded_at
      - *is_smart_contract
      - *is_eoa
      - *_updated_at

  - name: addresses_hyperevm_first_funding
    meta:
      blockchain: hyperevm
      sector: addresses
      contributors: kryptaki
    config:
      tags: ['address', 'first_funding']
    description: "Per-address first funding for HyperEVM"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address
    columns:
      - *address
      - *blockchain
      - *chain_id
      - *first_funded_by
      - *first_funded_at
      - *is_smart_contract
      - *is_eoa
      - *_updated_at

  - name: addresses_sei_first_funding
    meta:
      blockchain: sei
      sector: addresses
      contributors: kryptaki
    config:
      tags: ['address', 'first_funding']
    description: "Per-address first funding for Sei"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address
    columns:
      - *address
      - *blockchain
      - *chain_id
      - *first_funded_by
      - *first_funded_at
      - *is_smart_contract
      - *is_eoa
      - *_updated_at
