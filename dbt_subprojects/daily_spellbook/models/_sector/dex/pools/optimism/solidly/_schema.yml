version: 2

models:
  - name:  solidly_pools_optimism_balances
    description: "Tracks OP token balances in solidly pools on Optimism."
    meta:
      blockchain: optimism
      sector: DEX
      project: solidly
      contributors: jason
    config:
      tags: ['optimism', 'op_token', 'balances', 'solidly']
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - pool_address
            - snapshot_day