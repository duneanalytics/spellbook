version: 2

models:
  - name:  swaap_pools_optimism_balances
    description: "Tracks OP token balances in swaap pools on Optimism."
    meta:
      blockchain: optimism
      sector: DEX
      project: swaap
      contributors: jason
    config:
      tags: ['optimism', 'op_token', 'balances', 'swaap']
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - pool_address
            - snapshot_day