version: 2

models:
  - name: addresses_ethereum_stg_info
    meta:
      blockchain: ethereum
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for Ethereum. No target join - feeds addresses_ethereum_info merge."

  - name: addresses_ethereum_info
    meta:
      blockchain: ethereum
      sector: addresses
      contributors: hildobby
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on Ethereum"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - &blockchain
        name: blockchain
        description: "Blockchain"
      - &address
        name: address
        description: "Address"
      - &address_prefix
        name: address_prefix
        description: "First byte of address as integer (0-255), for partitioning and join optimization"
      - &executed_tx_count
        name: executed_tx_count
        description: "The number of transactions executed by the address"
      - &max_nonce
        name: max_nonce
        description: "The highest nonce value"
      - &is_smart_contract
        name: is_smart_contract
        description: "Is the address a smart contract"
      - &namespace
        name: namespace
        description: "If the address is a smart contract that is decoded on Dune, here is the associated namespace on Dune"
      - &name
        name: name
        description: "If the address is a smart contract that is decoded on Dune, here is the associated smart contract name on Dune"
      - &first_funded_by
        name: first_funded_by
        description: "Address that first funded this address with this chains' native token"
      - &first_funded_by_block_time
        name: first_funded_by_block_time
        description: "Block time at which the address was first funded with this chains' native token"
      - &tokens_received_count
        name: tokens_received_count
        description: "The number of times this address received tokens"
      - &tokens_received_tx_count
        name: tokens_received_tx_count
        description: "The distinct transactions where this address received tokens"
      - &tokens_sent_count
        name: tokens_sent_count
        description: "The number of times this address sent tokens"
      - &tokens_sent_tx_count
        name: tokens_sent_tx_count
        description: "The distinct transactions where this address sent tokens"
      - &first_transfer_block_time
        name: first_transfer_block_time
        description: "The block time of the first time this address sent or received tokens"
      - &last_transfer_block_time
        name: last_transfer_block_time
        description: "The block time of the last time this address sent or received tokens"
      - &first_received_block_number
        name: first_received_block_number
        description: "The block number of the first time this address received tokens"
      - &last_received_block_number
        name: last_received_block_number
        description: "The block number of the last time this address received tokens"
      - &first_sent_block_number
        name: first_sent_block_number
        description: "The block number of the first time this address sent tokens"
      - &last_sent_block_number
        name: last_sent_block_number
        description: "The block number of the last time this address sent tokens"
      - &received_volume_usd
        name: received_volume_usd
        description: "The USD volume of tokens received by this address"
      - &sent_volume_usd
        name: sent_volume_usd
        description: "The USD volume of tokens sent by this address"
      - &first_tx_block_time
        name: first_tx_block_time
        description: "The block time of the first transaction executed by this address"
      - &last_tx_block_time
        name: last_tx_block_time
        description: "The block time of the last transaction executed by this address"
      - &first_tx_block_number
        name: first_tx_block_number
        description: "The block number of the first transaction executed by this address"
      - &last_tx_block_number
        name: last_tx_block_number
        description: "The block number of the last transaction executed by this address"
      - &last_seen
        name: last_seen
        description: "The time at which this address last appeared in any form on the blockchain"
      - &last_seen_block
        name: last_seen_block
        description: "The last block number this address was seen on"

  - name: addresses_arbitrum_stg_info
    meta:
      blockchain: arbitrum
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for Arbitrum. No target join."

  - name: addresses_arbitrum_info
    meta:
      blockchain: arbitrum
      sector: addresses
      contributors: hildobby
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on Arbitrum"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block

  - name: addresses_avalanche_c_stg_info
    meta:
      blockchain: avalanche_c
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for Avalanche. No target join."

  - name: addresses_avalanche_c_info
    meta:
      blockchain: avalanche_c
      sector: addresses
      contributors: hildobby
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on Avalanche"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block

  - name: addresses_base_stg_info
    meta:
      blockchain: base
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for Base. No target join."

  - name: addresses_base_info
    meta:
      blockchain: base
      sector: addresses
      contributors: hildobby
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on Base"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block

  - name: addresses_blast_stg_info
    meta:
      blockchain: blast
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for Blast. No target join."

  - name: addresses_blast_info
    meta:
      blockchain: blast
      sector: addresses
      contributors: hildobby
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on Blast"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block

  - name: addresses_bnb_stg_info
    meta:
      blockchain: bnb
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for BNB. No target join."

  - name: addresses_bnb_info
    meta:
      blockchain: bnb
      sector: addresses
      contributors: hildobby
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on BNB"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block

  - name: addresses_celo_stg_info
    meta:
      blockchain: celo
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for Celo. No target join."

  - name: addresses_celo_info
    meta:
      blockchain: celo
      sector: addresses
      contributors: hildobby
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on Celo"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block
      
  - name: addresses_gnosis_stg_info
    meta:
      blockchain: gnosis
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for Gnosis. No target join."

  - name: addresses_gnosis_info
    meta:
      blockchain: gnosis
      sector: addresses
      contributors: hildobby
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on Gnosis"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block

  - name: addresses_optimism_stg_info
    meta:
      blockchain: optimism
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for Optimism. No target join."

  - name: addresses_optimism_info
    meta:
      blockchain: optimism
      sector: addresses
      contributors: hildobby
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on Optimism"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block

  - name: addresses_polygon_stg_info
    meta:
      blockchain: polygon
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for Polygon. No target join."

  - name: addresses_polygon_info
    meta:
      blockchain: polygon
      sector: addresses
      contributors: hildobby
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on Polygon"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block

  - name: addresses_scroll_stg_info
    meta:
      blockchain: scroll
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for Scroll. No target join."

  - name: addresses_scroll_info
    meta:
      blockchain: scroll
      sector: addresses
      contributors: hildobby
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on Scroll"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block

  - name: addresses_zora_stg_info
    meta:
      blockchain: zora
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for Zora. No target join."

  - name: addresses_zora_info
    meta:
      blockchain: zora
      sector: addresses
      contributors: hildobby
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on Zora"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block

  - name: addresses_hyperevm_stg_info
    meta:
      blockchain: hyperevm
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for HyperEVM. No target join."

  - name: addresses_hyperevm_info
    meta:
      blockchain: hyperevm
      sector: addresses
      contributors: kyptaki
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on HyperEVM"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block

  - name: addresses_monad_stg_info
    meta:
      blockchain: monad
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for Monad. No target join."

  - name: addresses_monad_info
    meta:
      blockchain: monad
      sector: addresses
      contributors: kryptaki
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on Monad"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block

  - name: addresses_sei_stg_info
    meta:
      blockchain: sei
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for Sei. No target join."

  - name: addresses_sei_info
    meta:
      blockchain: sei
      sector: addresses
      contributors: kryptaki
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on Sei"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block

  - name: addresses_fantom_stg_info
    meta:
      blockchain: fantom
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for Fantom. No target join."

  - name: addresses_fantom_info
    meta:
      blockchain: fantom
      sector: addresses
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on Fantom"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block

  - name: addresses_linea_stg_info
    meta:
      blockchain: linea
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for Linea. No target join."

  - name: addresses_linea_info
    meta:
      blockchain: linea
      sector: addresses
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on Linea"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block

  - name: addresses_zkevm_stg_info
    meta:
      blockchain: zkevm
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for zkEVM. No target join."

  - name: addresses_zkevm_info
    meta:
      blockchain: zkevm
      sector: addresses
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on zkEVM"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block

  - name: addresses_zksync_stg_info
    meta:
      blockchain: zksync
      sector: addresses
    config:
      tags: ['staging', 'address']
    description: "Staging: address activity in incremental window for zkSync. No target join."

  - name: addresses_zksync_info
    meta:
      blockchain: zksync
      sector: addresses
    config:
      tags: ['table', 'address', 'info']
    description: "High level information about every address on zkSync"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address_prefix
            - address
    columns:
      - *blockchain
      - *address
      - *address_prefix
      - *executed_tx_count
      - *max_nonce
      - *is_smart_contract
      - *namespace
      - *name
      - *first_funded_by
      - *first_funded_by_block_time
      - *tokens_received_count
      - *tokens_received_tx_count
      - *tokens_sent_count
      - *tokens_sent_tx_count
      - *first_transfer_block_time
      - *last_transfer_block_time
      - *first_received_block_number
      - *last_received_block_number
      - *first_sent_block_number
      - *last_sent_block_number
      - *received_volume_usd
      - *sent_volume_usd
      - *first_tx_block_time
      - *last_tx_block_time
      - *first_tx_block_number
      - *last_tx_block_number
      - *last_seen
      - *last_seen_block