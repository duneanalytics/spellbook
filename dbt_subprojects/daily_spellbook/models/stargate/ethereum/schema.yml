version: 2

models:
  - name: pre_post_bridge_ethereum
    description: "Pre and post transaction context around Stargate bridge usage on Ethereum."
    columns:
      - name: user
        tests: [not_null]
      - name: bridge_tx_hash
        tests: [not_null]
      - name: bridge_block_number
        tests: [not_null]
      - name: bridge_block_time
        tests: [not_null]
