{% set blockchain = 'polygon' %}

{{ config(
        schema = 'metrics_' + blockchain
        , alias = 'transfers_daily_asset'
        , materialized = 'view'
        )
}}

SELECT *
FROM {{ source('tokens_polygon', 'net_transfers_daily_asset') }}
