version: 2

models:
  - name: metrics_transactions_daily
    meta:
      sector: metrics
      contributors: jeff-dude
    config:
      tags: ['metrics', 'transactions', 'daily']
    description: "Sum of total tx's per day across all chains"
  - name: metrics_transactions_daily_address
    meta:
      sector: metrics
      contributors: jeff-dude
    config:
      tags: ['metrics', 'transactions', 'daily']
    description: "Sum of total tx's per day per address across all chains"

  - name: metrics_transactions_stats
    meta:
      sector: metrics
      contributors: jeff-dude
    config:
      tags: [ 'metrics', 'transactions' ]
    description: "View of tx's per blockchain aggregated to various levels. The goal is to output one row per chain."
