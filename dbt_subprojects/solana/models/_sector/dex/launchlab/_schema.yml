version: 3

models:
  - name: raydium_launchlab_base_trades
    meta:
      blockchain: solana
      contributors: [krishhh]
    config:
      tags: ['solana', 'dex']
    description: >
      all raw raydium launchlab dex trades on Solana
    data_tests:
      - check_columns_solana_dex_trades
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns: ['tx_id', 'outer_instruction_index', 'inner_instruction_index', 'tx_index', 'block_month']

