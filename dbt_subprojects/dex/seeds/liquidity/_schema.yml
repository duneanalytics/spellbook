version: 2
seeds:
  - name: uniswap_ethereum_base_liquidity_seed
    config:
      column_types:
        blockchain: varchar
        project: varchar
        version: varchar
        tx_hash: varbinary
        evt_index: uint256
        block_number: uint256
        id: varbinary
        salt: varbinary
        token0: varbinary
        token1: varbinary
        amount0_raw: double
        amount1_raw: double
        block_date: timestamp

