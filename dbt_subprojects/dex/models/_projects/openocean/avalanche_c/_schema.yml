version: 2

models:
  - name: openocean_avalanche_c_base_trades
    meta:
      blockchain: avalanche_c
      sector: dex
      project: openocean
      contributors: thevaizman
    config:
      tags: [ 'avalanche_c', 'openocean', 'trades', 'dex', 'aggregator' ]
    description: >
        openocean aggregator trades on avalanche_c
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - block_date
              - blockchain
              - project
              - version
              - tx_hash
              - evt_index
              - trace_address
      - check_dex_aggregator_seed:
          arguments:
            blockchain: avalanche_c
            project: openocean
            version: 2
    columns:
      - name: blockchain
        description: "Blockchain which the DEX aggregator is deployed"
      - name: project
        description: "Project name of the DEX aggregator"
      - name: version
        description: "Version of the contract built and deployed by the DEX aggregator project"
      - name: block_date
        description: "UTC event block date of each DEX trade"
      - name: block_month
        description: "UTC event block month of each DEX trade"
      - name: block_time
        description: "UTC event block time of each DEX trade"
      - name: token_bought_amount_raw
        description: "Raw value of the token bought at time of execution in the original currency"
      - name: token_sold_amount_raw
        description: "Raw value of the token sold at time of execution in the original currency"
      - name: token_bought_address
        description: "Contract address of the token bought"
      - name: token_sold_address
        description: "Contract address of the token sold"
      - name: taker
        description: "Address of trader who purchased a token"
      - name: maker
        description: "Address of trader who sold a token"
      - name: project_contract_address
        description: "Project contract address which executed the trade on the blockchain"
      - name: tx_hash
        description: "Unique transaction hash value tied to each transaction on the DEX"
      - name: tx_from
        description: "Address which initiated the transaction"
      - name: tx_to
        description: "Address which received the transaction"
      - name: trace_address
        description: "Address of the trace within the call graph forest"
      - name: evt_index
        description: "The position of this event log within this transaction"
