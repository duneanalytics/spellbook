version: 2

models:
  - name: tokens_flare_erc20
    meta:
      blockchain: flare
      sector: tokens
      project: erc20
      contributors: yakufff
    config:
      tags: ['table', 'metadata', 'erc20', 'flare']
    description: "ERC20 Token Addresses, Symbols and Decimals"
    columns:
      - name: contract_address
        description: "ERC20 token contract address on flare"
        tests:
          - unique
      - name: symbol
        description: "ERC20 token symbol"
      - name: decimals
        description: "Number of decimals, refers to how divisible an ERC20 token can be"