version: 2

models:
  - name: tokens_gnosis_erc20_stablecoins
    meta:
      blockchain: gnosis
      sector: stablecoins
      contributors: synthquest, tomfutago
    config:
      tags: ['tokens', 'gnosis', 'erc20', 'stablecoins']
    description: >
        Union view of all stablecoin token addresses (core + extended).
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - contract_address
    columns:
      - &blockchain
        name: blockchain
        description: "Blockchain name"
      - &contract_address
        name: contract_address
        description: "Stablecoin contract address"

  - name: tokens_gnosis_erc20_stablecoins_core
    meta:
      blockchain: gnosis
      sector: stablecoins
      contributors: synthquest, tomfutago
    config:
      tags: ['tokens', 'gnosis', 'erc20', 'stablecoins']
    description: >
        Core list of stablecoin token addresses (frozen).
    columns:
      - *blockchain
      - *contract_address

  - name: tokens_gnosis_erc20_stablecoins_extended
    meta:
      blockchain: gnosis
      sector: stablecoins
      contributors: tomfutago
    config:
      tags: ['tokens', 'gnosis', 'erc20', 'stablecoins']
    description: >
        Latest additions to stablecoin token addresses.
    columns:
      - *blockchain
      - *contract_address
