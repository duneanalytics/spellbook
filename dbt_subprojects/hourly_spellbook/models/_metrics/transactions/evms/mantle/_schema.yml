version: 2

models:
  - name: mantle_address_metrics
    columns:
      - name: blockchain
      - name: address
        tests:
          - unique
      - name: min_block_time
        description: "block_time of the first transaction initiated by the address"

  - name: mantle_transaction_metrics
    meta:
      blockchain: mantle
      sector: metrics
      contributors: 0xRob
    config:
      tags: [ 'mantle','metrics' ]
    description: "fundamental blockchain transaction metrics"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - blockchain
            - block_hour

  - name: mantle_transaction_address_metrics
    meta:
      blockchain: mantle
      sector: metrics
      contributors: jeff-dude
    config:
      tags: [ 'mantle','metrics', 'address' ]
    description: "fundamental blockchain transaction address metrics"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - blockchain
            - block_hour
            - from_address
            - to_address