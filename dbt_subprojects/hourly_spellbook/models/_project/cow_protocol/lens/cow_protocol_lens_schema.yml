version: 2

models:
  - name: cow_protocol_lens_solvers
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - address
    meta:
      blockchain: lens
      project: cow_protocol
      contributors: harisang
    config:
      tags: ["lens", "cow_protocol", "solver"]
    description: >
      CoW Protocol solvers list on Lens Chain

  - name: cow_protocol_lens_batches
    meta:
      blockchain: lens
      project: cow_protocol
      contributors: harisang
    config:
      tags: ["lens", "cow_protocol", "trades", "dex", "aggregator", "auction"]
    description: >
      CoW Protocol enriched batches table on Lens Chain
    data_tests:
      - unique:
          column_name: tx_hash
