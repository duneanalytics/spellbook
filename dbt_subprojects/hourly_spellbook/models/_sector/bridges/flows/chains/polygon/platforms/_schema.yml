version: 2

models:
  - name: bridges_polygon_cctp_v1_deposits
    meta:
      blockchain: polygon
      sector: bridges
      project: cctp
      contributors: [ 'hildobby']
    config:
      tags: [ 'polygon', 'bridges', 'flows', 'deposits' ]
    description: "Circle's CCTP v1 bridge deposits events on Polygon"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns: ['tx_hash','evt_index']
      - equal_rowcount_with_sources:
          evt_sources:
            - source('circle_polygon','tokenmessenger_evt_depositforburn')
  - name: bridges_polygon_cctp_v1_withdrawals
    meta:
      blockchain: polygon
      sector: bridges
      project: cctp
      contributors: [ 'hildobby']
    config:
      tags: [ 'polygon', 'bridges', 'flows', 'withdrawals' ]
    description: "Circle's CCTP v1 bridge withdrawals events on Polygon"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns: ['tx_hash','evt_index']
      - equal_rowcount_with_sources:
          evt_sources:
            - source('circle_polygon','tokenmessenger_evt_mintandwithdraw')
  - name: bridges_polygon_across_v2_deposits
    meta:
      blockchain: polygon
      sector: bridges
      project: across
      contributors: [ 'hildobby']
    config:
      tags: [ 'across', 'bridges', 'flows' , 'deposits' ]
    description: "Across's v2 bridge deposits events on Polygon"
  - name: bridges_polygon_across_v2_withdrawals
    meta:
      blockchain: polygon
      sector: bridges
      project: across
      contributors: [ 'hildobby']
    config:
      tags: [ 'across', 'bridges', 'flows' , 'withdrawals' ]
    description: "Across's v3 bridge withdrawals events on Polygon"
  - name: bridges_polygon_across_v3_deposits
    meta:
      blockchain: polygon
      sector: bridges
      project: across
      contributors: [ 'hildobby']
    config:
      tags: [ 'across', 'bridges', 'flows' , 'deposits' ]
    description: "Across's v3 bridge deposits events on Polygon"
  - name: bridges_polygon_across_v3_withdrawals
    meta:
      blockchain: polygon
      sector: bridges
      project: across
      contributors: [ 'hildobby']
    config:
      tags: [ 'across', 'bridges', 'flows' , 'withdrawals' ]
    description: "Across's v3 bridge withdrawals events on Polygon"
  - name: bridges_polygon_celer_v1_deposits
    meta:
      blockchain: polygon
      sector: bridges
      project: celer
      contributors: [ 'hildobby']
    config:
      tags: [ 'celer', 'bridges', 'flows' , 'deposits' ]
    description: "Celer's v1 bridge deposits events on Polygon"
  - name: bridges_polygon_celer_v1_withdrawals
    meta:
      blockchain: polygon
      sector: bridges
      project: celer
      contributors: [ 'hildobby']
    config:
      tags: [ 'celer', 'bridges', 'flows' , 'withdrawals' ]
    description: "Celer's v1 bridge withdrawals events on Polygon"
            
