version: 2

models:
  - name: bridges_solana_across_v3_deposits
    meta:
      blockchain: solana
      sector: bridges
      project: across
      contributors: [ 'hildobby']
    config:
      tags: [ 'across', 'bridges', 'flows' , 'deposits' ]
    description: "Across's v3 bridge deposits events on Solana"
  - name: bridges_solana_across_v3_withdrawals
    meta:
      blockchain: solana
      sector: bridges
      project: across
      contributors: [ 'hildobby']
    config:
      tags: [ 'across', 'bridges', 'flows' , 'withdrawals' ]
    description: "Across's v3 bridge withdrawals events on Solana"