version: 2

models:
  - name: bridges_cctp_chain_indexes
    meta:
      blockchain: ethereum
      sector: bridges
      project: cctp
      contributors: [ 'hildobby']
    config:
      tags: [ 'bridges', 'chain_indexes' ]
    description: "Circle's CCTP blockchains indexes"
    columns:
      - &id
        name: id
        description: "Blockchain id"
      - &blockchain
        name: blockchain
        description: "Blockchain name"
  - name: bridges_across_chain_indexes
    meta:
      blockchain: ethereum
      sector: bridges
      project: across
      contributors: [ 'hildobby']
    config:
      tags: [ 'bridges', 'chain_indexes' ]
    description: "Across's blockchains indexes"
    columns:
      - *id
      - *blockchain
  - name: bridges_tether_chain_indexes
    meta:
      blockchain: ethereum
      sector: bridges
      project: tether
      contributors: [ 'hildobby']
    config:
      tags: [ 'bridges', 'chain_indexes' ]
    description: "Tether's blockchains indexes"
    columns:
      - *id
      - *blockchain
      - &usdt0_address
        name: usdt0_address
        description: "Contract address of USDT0 per chain"
  - name: bridges_connext_chain_indexes
    meta:
      blockchain: ethereum
      sector: bridges
      project: connext
      contributors: [ 'hildobby']
    config:
      tags: [ 'bridges', 'chain_indexes' ]
    description: "Connext's blockchains indexes"
    columns:
      - *id
      - *blockchain
  - name: bridges_zkbridge_chain_indexes
    meta:
      blockchain: ethereum
      sector: bridges
      project: zkbridge
      contributors: [ 'hildobby']
    config:
      tags: [ 'bridges', 'chain_indexes' ]
    description: "zkBridge's blockchains indexes"
    columns:
      - *id
      - *blockchain
  - name: bridges_zkbridge_token_indexes
    meta:
      blockchain: ethereum
      sector: bridges
      project: zkbridge
      contributors: [ 'hildobby']
    config:
      tags: [ 'bridges', 'chain_indexes' ]
    description: "zkBridge's blockchains indexes"
    columns:
      - *blockchain
      - &contract_address
        name: contract_address
        description: "Address of the contract used to bridge the token"
      - &pool_id
        name: pool_id
        description: "ID of the pool used to bridge the token"
      - &token_address
        name: token_address
        description: "Address of the token in the pool"
  - name: bridges_symbiosis_chain_indexes
    meta:
      blockchain: ethereum
      sector: bridges
      project: symbiosis
      contributors: [ 'hildobby']
    config:
      tags: [ 'bridges', 'chain_indexes' ]
    description: "Symbiosis's blockchains indexes"
    columns:
      - *id
      - *blockchain
  - name: bridges_axelar_ist_token_indexes
    meta:
      blockchain: ethereum
      sector: bridges
      project: axelar
      contributors: [ 'hildobby']
    config:
      tags: [ 'bridges', 'token_indexes' ]
    description: "Axelar's token indexes for its ITS bridge"
    columns:
      - &token_id
        name: token_id
        description: "Token id"
      - *blockchain
      - *token_address
      - &token_symbol
        name: token_symbol
        description: "Token symbol"
  - name: bridges_axelar_gateway_token_indexes
    meta:
      blockchain: ethereum
      sector: bridges
      project: axelar
      contributors: [ 'hildobby']
    config:
      tags: [ 'bridges', 'token_indexes' ]
    description: "Axelar's token indexes for its gateway bridge"
    columns:
      - *blockchain
      - *token_symbol
      - *token_address
  - name: bridges_orbiter_chain_indexes
    meta:
      blockchain: ethereum
      sector: bridges
      project: orbiter
      contributors: [ 'hildobby']
    config:
      tags: [ 'bridges', 'chain_indexes' ]
    description: "Orbiter's blockchains indexes"
    columns:
      - *id
      - *blockchain
  - name: bridges_layerzero_chain_indexes
    meta:
      blockchain: ethereum
      sector: bridges
      project: layerzero
      contributors: [ 'hildobby']
    config:
      tags: [ 'bridges', 'chain_indexes' ]
    description: "LayerZero's blockchains indexes"
    columns:
      - *blockchain
      - *id
      - &endpoint_address
        name: endpoint_address
        description: "Endpoint address"
  - name: bridges_agglayer_chain_indexes
    meta:
      blockchain: ethereum
      sector: bridges
      project: agglayer
      contributors: [ 'hildobby']
    config:
      tags: [ 'bridges', 'chain_indexes' ]
    description: "Agglayer's blockchains indexes"
    columns:
      - *id
      - *blockchain
