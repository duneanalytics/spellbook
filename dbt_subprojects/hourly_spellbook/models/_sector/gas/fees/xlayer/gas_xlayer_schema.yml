version: 2

models:
  - name: gas_xlayer_fees
    meta:
      blockchain: xlayer
      sector: gas
      contributors: tomfutago
    config:
      tags: ['xlayer', 'gas', 'fees']
    description: Gas fees on xlayer
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - block_month
            - tx_hash
