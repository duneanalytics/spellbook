version: 2

models:
  - name: lending_tron_base_supply
    meta:
      blockchain: tron
      sector: lending
      project: justlend
      contributors: tomfutago, codex
    config:
      tags: ['lending', 'supply', 'justlend', 'tron']
    description: "All lending supply transactions on tron"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - transaction_type
            - token_address
            - tx_hash
            - evt_index
