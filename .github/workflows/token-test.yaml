name: runner token test

on:
  pull_request:
    paths:
      - .github/workflows/test.yaml

jobs:
  runner-test:
    runs-on: [ self-hosted, linux, spellbook-trino-ci ]
    steps:
      - name: Print runner token hash
        run: echo $RUNNER_TOKEN | sha256sum
