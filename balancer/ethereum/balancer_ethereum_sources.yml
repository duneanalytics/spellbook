
#ERC Transfer Tables

-name: erc20_ethereum
description: "These are transfer events for ERC20 Tokens"
tables:
    -name: evt_transfer
    loaded_at_field: evt_block_time
    freshness: 
      warn_after: {count: 12, period: hour}
      error_after: {count: 24, period: hour}
    columns:
      -name: contract_address # add descriptions blablabla ...

  #Also the bare minimum is allowed

  -name : balancer_ethereum
   tables:
      -name: BFactory_evt_LOG_NEW_POOL