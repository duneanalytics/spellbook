version: 2

models:
  - name: nft_arbitrum_aggregators
    meta:
      blockchain: arbitrum
      sector: nft
      contributors: hildobby
    config:
      tags: ['nft', 'arbitrum', 'aggregators']
    description: >
        NFT aggregators
    columns:
      - name: contract_address
        description: "Aggregator project's contract address"
        tests:
          - unique
      - name: name
        description: "Aggregator project's name"
