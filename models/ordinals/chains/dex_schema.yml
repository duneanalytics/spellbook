version: 2

models:
  - name: ordinals_ethereum_mints
    meta: 
      blockchain: ethereum
      sector: dex
      contributors: hildobby
    config:
      tags: ['ordinals', 'mints', 'ethereum']
    description: >
      Inscription mints on Ethereum
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - blockchain
            - tx_hash
    columns: 
      - &blockchain
        name: blockchain
        description: "Blockchain"
      - &block_time
        name: block_time
        description: "UTC event block time of each DEX trade"
      - &block_month
        name: block_month
        description: "UTC event block month of each DEX trade"
      - &block_number
        name: block_number
        description: "the block number of the block that the offer was created in"
      - &tx_hash
        name: tx_hash
        description: "Unique transaction hash value tied to each transaction on the DEX"
      - &tx_index
        name: tx_index
        description: "Index of the transaction in block"
      - &tx_from
        name: tx_from
        description: "Address which initiated the trade"
      - &tx_to
        name: tx_to
        description: "Address which received the trade"
      - &ordinal_standard
        name: ordinal_standard
        description: "Ordinal standard"
      - &ordinal_symbol
        name: ordinal_symbol
        description: "Symbol of inscription"
      - &operation
        name: operation
        description: "Operation"
      - &amount
        name: amount
        description: "Amount of ordinals"
      - &full_inscription
        name: full_inscription
        description: "Full inscription information as found in the transaction's call data"
      - &vin
        name: vin
        description: "Input of transaction"
      - &vout
        name: vout
        description: "Output of transaction"

  - name: ordinals_arbitrum_mints
    meta: 
      blockchain: arbitrum
      sector: dex
      contributors: hildobby
    config:
      tags: ['ordinals', 'mints', 'arbitrum']
    description: >
      Inscription mints on Arbitrum
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - blockchain
            - tx_hash
    columns: 
      - *blockchain
      - *block_time
      - *block_month
      - *block_number
      - *tx_hash
      - *tx_index
      - *tx_from
      - *tx_to
      - *tx_index
      - *ordinal_standard
      - *operation
      - *ordinal_symbol
      - *amount
      - *full_inscription
      - *vin
      - *vout

  - name: ordinals_avalanche_c_mints
    meta: 
      blockchain: avalanche_c
      sector: dex
      contributors: hildobby
    config:
      tags: ['ordinals', 'mints', 'avalanche_c']
    description: >
      Inscription mints on Avalanche
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - blockchain
            - tx_hash
    columns: 
      - *blockchain
      - *block_time
      - *block_month
      - *block_number
      - *tx_hash
      - *tx_index
      - *tx_from
      - *tx_to
      - *tx_index
      - *ordinal_standard
      - *operation
      - *ordinal_symbol
      - *amount
      - *full_inscription
      - *vin
      - *vout

  - name: ordinals_bnb_mints
    meta: 
      blockchain: bnb
      sector: dex
      contributors: hildobby
    config:
      tags: ['ordinals', 'mints', 'bnb']
    description: >
      Inscription mints on BNB
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - blockchain
            - tx_hash
    columns: 
      - *blockchain
      - *block_time
      - *block_month
      - *block_number
      - *tx_hash
      - *tx_index
      - *tx_from
      - *tx_to
      - *tx_index
      - *ordinal_standard
      - *operation
      - *ordinal_symbol
      - *amount
      - *full_inscription
      - *vin
      - *vout

  - name: ordinals_fantom_mints
    meta: 
      blockchain: fantom
      sector: dex
      contributors: hildobby
    config:
      tags: ['ordinals', 'mints', 'fantom']
    description: >
      Inscription mints on Fantom
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - blockchain
            - tx_hash
    columns: 
      - *blockchain
      - *block_time
      - *block_month
      - *block_number
      - *tx_hash
      - *tx_index
      - *tx_from
      - *tx_to
      - *tx_index
      - *ordinal_standard
      - *operation
      - *ordinal_symbol
      - *amount
      - *full_inscription
      - *vin
      - *vout

  - name: ordinals_gnosis_mints
    meta: 
      blockchain: gnosis
      sector: dex
      contributors: hildobby
    config:
      tags: ['ordinals', 'mints', 'gnosis']
    description: >
      Inscription mints on Gnosis
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - blockchain
            - tx_hash
    columns: 
      - *blockchain
      - *block_time
      - *block_month
      - *block_number
      - *tx_hash
      - *tx_index
      - *tx_from
      - *tx_to
      - *tx_index
      - *ordinal_standard
      - *operation
      - *ordinal_symbol
      - *amount
      - *full_inscription
      - *vin
      - *vout

  - name: ordinals_optimism_mints
    meta: 
      blockchain: optimism
      sector: dex
      contributors: hildobby
    config:
      tags: ['ordinals', 'mints', 'optimism']
    description: >
      Inscription mints on Optimism
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - blockchain
            - tx_hash
    columns: 
      - *blockchain
      - *block_time
      - *block_month
      - *block_number
      - *tx_hash
      - *tx_index
      - *tx_from
      - *tx_to
      - *tx_index
      - *ordinal_standard
      - *operation
      - *ordinal_symbol
      - *amount
      - *full_inscription
      - *vin
      - *vout

  - name: ordinals_polygon_mints
    meta: 
      blockchain: polygon
      sector: dex
      contributors: hildobby
    config:
      tags: ['ordinals', 'mints', 'polygon']
    description: >
      Inscription mints on Polygon
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - blockchain
            - tx_hash
    columns: 
      - *blockchain
      - *block_time
      - *block_month
      - *block_number
      - *tx_hash
      - *tx_index
      - *tx_from
      - *tx_to
      - *tx_index
      - *ordinal_standard
      - *operation
      - *ordinal_symbol
      - *amount
      - *full_inscription
      - *vin
      - *vout

  - name: ordinals_base_mints
    meta: 
      blockchain: base
      sector: dex
      contributors: hildobby
    config:
      tags: ['ordinals', 'mints', 'base']
    description: >
      Inscription mints on Base
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - blockchain
            - tx_hash
    columns: 
      - *blockchain
      - *block_time
      - *block_month
      - *block_number
      - *tx_hash
      - *tx_index
      - *tx_from
      - *tx_to
      - *tx_index
      - *ordinal_standard
      - *operation
      - *ordinal_symbol
      - *amount
      - *full_inscription
      - *vin
      - *vout

  - name: ordinals_celo_mints
    meta: 
      blockchain: celo
      sector: dex
      contributors: hildobby
    config:
      tags: ['ordinals', 'mints', 'celo']
    description: >
      Inscription mints on Celo
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - blockchain
            - tx_hash
    columns: 
      - *blockchain
      - *block_time
      - *block_month
      - *block_number
      - *tx_hash
      - *tx_index
      - *tx_from
      - *tx_to
      - *tx_index
      - *ordinal_standard
      - *operation
      - *ordinal_symbol
      - *amount
      - *full_inscription
      - *vin
      - *vout

  - name: ordinals_zksync_mints
    meta: 
      blockchain: zksync
      sector: dex
      contributors: hildobby
    config:
      tags: ['ordinals', 'mints', 'zksync']
    description: >
      Inscription mints on zkSync
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - blockchain
            - tx_hash
    columns: 
      - *blockchain
      - *block_time
      - *block_month
      - *block_number
      - *tx_hash
      - *tx_index
      - *tx_from
      - *tx_to
      - *tx_index
      - *ordinal_standard
      - *operation
      - *ordinal_symbol
      - *amount
      - *full_inscription
      - *vin
      - *vout

  - name: ordinals_zora_mints
    meta: 
      blockchain: zora
      sector: dex
      contributors: hildobby
    config:
      tags: ['ordinals', 'mints', 'zora']
    description: >
      Inscription mints on Zora Network
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - blockchain
            - tx_hash
    columns: 
      - *blockchain
      - *block_time
      - *block_month
      - *block_number
      - *tx_hash
      - *tx_index
      - *tx_from
      - *tx_to
      - *tx_index
      - *ordinal_standard
      - *operation
      - *ordinal_symbol
      - *amount
      - *full_inscription
      - *vin
      - *vout
      