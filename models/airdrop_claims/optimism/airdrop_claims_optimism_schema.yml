version: 2

models:
  - name: airdrop_claims_optimism_optimism_1
    meta:
      blockchain: optimism
      sector: airdrop_claim
      project: addresses
      contributors: hildobby
    config:
      tags: ['table', 'airdrop', 'optimism', 'claim']
    description: "List of claim transactions for the $OP airdrop"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - recipient
    columns:
      - &blockchain
        name: blockchain
        description: "Blockchain"
      - &block_time
        name: block_time
        description: "Block time of transaction"
      - &block_number
        name: block_number
        description: "Block number of transaction"
      - &project
        name: project
        description: "Project name"
      - &airdrop_identifier
        name: airdrop_identifier
        description: "Airdrop identifier name"
      - &recipient
        name: recipient
        description: "Airdrop recipient"
      - &contract_address
        name: contract_address
        description: "Contract addressed used"
      - &tx_hash
        name: tx_hash
        description: "Transaction hash"
      - &quantity
        name: quantity
        description: "Quantity of claim tokens"
      - &token_address
        name: token_address
        description: "Token address"
      - &token_symbol
        name: token_symbol
        description: "Token symbol"
      - &evt_index
        name: evt_index
        description: "Event index"

  - name: airdrop_claims_optimism_velodrome
    meta:
      blockchain: optimism
      sector: airdrop_claim
      project: addresses
      contributors: hildobby
    config:
      tags: ['table', 'airdrop', 'optimism', 'velodrome','claim']
    description: "List of claim transactions for the $VELO airdrop"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - recipient
    columns:
      - *blockchain
      - *block_time
      - *block_number
      - *project
      - *airdrop_identifier
      - *recipient
      - *contract_address
      - *tx_hash
      - *quantity
      - *token_address
      - *token_symbol
      - *evt_index