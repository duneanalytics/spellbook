version: 2

models:

  - name: balances_base_eth_hour
    meta:
      blockchain: base
      sector: balances
      project: base
      contributors: rantum
    config:
      tags: ['balances', 'base', 'hour', 'rantum']
    description: >
        Hourly token balances of base for each address on base.
        Depends on transfers_base_eth
    columns:
      - *blockchain
      - &block_month
        name: block_month
        description: "UTC event block time truncated to the month mark"
      - *block_hour
      - *wallet_address
      - *token_address
      - *amount_raw
      - *amount
      - *amount_usd
      - *symbol

  - name: balances_base_eth_day
    meta:
      blockchain: base
      sector: balances
      project: base
      contributors: rantum
    config:
      tags: ['balances', 'base', 'day', 'rantum']
    description: >
        Daily token balances of base for each address on base.
        Depends on transfers_base_eth.
    columns:
      - *blockchain
      - *block_month
      - *block_day
      - *wallet_address
      - *token_address
      - *amount_raw
      - *amount
      - *amount_usd
      - *symbol


  - name: balances_base_eth_latest
    meta:
      blockchain: base
      sector: balances
      project: base
      contributors: rantum
    config:
      tags: ['balances', 'base', 'latest', 'rantum']
    description: >
        Latest token balances of base for each address on base.
        Depends on transfers_base_eth.
    columns:
      - *blockchain
      - *last_updated
      - *wallet_address
      - *token_address
      - *amount_raw
      - *amount
      - *amount_usd
      - *symbol
      