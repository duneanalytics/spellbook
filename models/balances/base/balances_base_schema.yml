version: 2

models:
  - name: balances_base_eth_hour
    meta:
      blockchain: base
      sector: balances
      project: eth
      contributors: rantum
    config:
      tags: ['balances', 'base', 'eth', 'hour', 'rantum']
    description: >
        Hourly token balances of eth base tokens per wallet and contract address pair.
        Depends on eth_base_transfers
    columns:
      - &blockchain
        name: blockchain
        description: "Blockchain"
      - &block_hour
        name: block_hour
        description: "UTC event block time truncated to the hour mark"
      - &wallet_address
        name: wallet_address
        description: "Address of the wallet holding this token"
      - &token_address
        name: token_address
        description: "Contract address for the token"
      - &amount_raw
        name: amount_raw
        description: "Raw amount of eth held *before* taking into account token decimals"
      - &amount
        name: amount
        description: "Raw amount of eth held *after* taking into account token decimals"
      - &amount_usd
        name: amount_usd
        description: "Amount of eth held in USD if we have it"
      - &symbol
        name: symbol
        description: "Token symbol"

  - name: balances_base_eth_day
    meta:
      blockchain: base
      sector: balances
      project: eth
      contributors: rantum
    config:
      tags: ['balances', 'base', 'eth', 'day', 'rantum']
    description: >
        Daily token balances of eth base tokens per wallet and contract address pair.
        Depends on eth_base_transfers.
    columns:
      - *blockchain
      - &block_day
        name: block_day
        description: "UTC event block time truncated to the day mark"
      - *wallet_address
      - *token_address
      - *amount_raw
      - *amount
      - *amount_usd
      - *symbol

  - name: balances_base_eth_latest
    meta:
      blockchain: base
      sector: balances
      project: eth
      contributors: rantum
    config:
      tags: ['balances', 'base', 'eth', 'latest', 'rantum']
    description: >
        Latest token balances of eth base tokens per wallet and contract address pair.
        Depends on eth_base_transfers.
    columns:
      - *blockchain
      - &last_updated
        name: last_updated
        description: "UTC timestamp when data was last updated"
      - *wallet_address
      - *token_address
      - *amount_raw
      - *amount
      - *amount_usd
      - *symbol

    meta:
      blockchain: base
      sector: balances
      project: base
      contributors: rantum
    config:
      tags: ['balances', 'base', 'latest', 'rantum']
    description: >
        Latest token balances of base for each address on base.
        Depends on transfers_base_eth.
    columns:
      - *blockchain
      - *last_updated
      - *wallet_address
      - *token_address
      - *amount_raw
      - *amount
      - *amount_usd
      - *symbol

