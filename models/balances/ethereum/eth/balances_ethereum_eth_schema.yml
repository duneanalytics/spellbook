version: 2

models:
  - name: balances_ethereum_eth_hour
    meta:
      blockchain: ethereum
      sector: balances
      project: ethereum
      contributors: rantum
    config:
      tags: ['balances', 'ethereum', 'ethereum', 'hour', 'rantum']
    description: >
        Hourly token balances of ethereum for each address on ethereum.
        Depends on transfers_ethereum_eth
    columns:
      - &blockchain
        name: blockchain
        description: "Blockchain"
      - &block_month
        name: block_month
        description: "UTC event block time truncated to the month mark"
      - &block_hour
        name: block_hour
        description: "UTC event block time truncated to the hour mark"
      - &wallet_address
        name: wallet_address
        description: "Address of the wallet holding ethereum"
      - &token_address
        name: token_address
        description: "ethereum Dead Address on ethereum"
      - &amount_raw
        name: amount_raw
        description: "Raw amount of ethereum token held *before* taking into account token decimals"
      - &amount
        name: amount
        description: "Raw amount of ethereum token held *after* taking into account token decimals"
      - &amount_usd
        name: amount_usd
        description: "Amount of ethereum token held in USD if we have it"
      - &symbol
        name: symbol
        description: "Token symbol"

  - name: balances_ethereum_eth_day
    meta:
      blockchain: ethereum
      sector: balances
      project: ethereum
      contributors: rantum
    config:
      tags: ['balances', 'ethereum', 'ethereum', 'day', 'rantum']
    description: >
        Daily token balances of ethereum for each address on ethereum.
        Depends on transfers_ethereum_eth.
    columns:
      - *blockchain
      - *block_month
      - &block_day
        name: block_day
        description: "UTC event block time truncated to the day mark"
      - *wallet_address
      - *token_address
      - *amount_raw
      - *amount
      - *amount_usd
      - *symbol


  - name: balances_ethereum_eth_latest
    meta:
      blockchain: ethereum
      sector: balances
      project: ethereum
      contributors: rantum
    config:
      tags: ['balances', 'ethereum', 'ethereum', 'latest', 'rantum']
    description: >
        Latest token balances of ethereum for each address on ethereum.
        Depends on transfers_ethereum_eth.
    columns:
      - *blockchain
      - &last_updated
        name: last_updated
        description: "UTC timestamp when data was last updated"
      - *wallet_address
      - *token_address
      - *amount_raw
      - *amount
      - *amount_usd
      - *symbol

