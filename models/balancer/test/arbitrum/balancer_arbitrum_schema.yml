version: 2

models:
   - name: balancer_arbitrum_test
    meta:
      blockchain: arbitrum
      sector: dex
      contributors: bizzyvinci
    config:
      tags: ['balancer', 'base', 'amm', 'trades', 'dex']
    description: >
      DEX trades on balancer on base
    columns:
      - name: test