version: 2

sources:
  - name: balancer_ethereum
    tables:
      - name: BFactory_evt_LOG_NEW_POOL
        freshness:
          warn_after:
            count: 12
            period: hour
        loaded_at_field: evt_block_time
