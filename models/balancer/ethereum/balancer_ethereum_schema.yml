version: 2

models:
 - name: balancer_ethereum_balances
   meta:
     blockchain: ethereum
     project: balancer
     contributors: katmphego
   config:
     tags: ['balances']
   description: >
        Balances on Balancer, an automated portfolio manager and trading platform on Ethereum.
   columns:
    - name: pool
      tests:
       - not_null
