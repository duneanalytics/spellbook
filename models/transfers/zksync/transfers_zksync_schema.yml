version: 2

models:
  - name: transfers_zksync_eth
    meta:
      blockchain: zksync
      sector: transfers
      project: eth
      contributors: lgingerich
    config:
      tags: ['transfers', 'zksync', 'eth']
    description: "Native ETH Transfers on zksync."
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - transfer_type
            - wallet_address
            - evt_tx_hash
            - evt_index
    columns:
      - &blockchain
        name: blockchain
        description: "Blockchain"
      - &transfer_type
        name: transfer_type
        description: "Transfer type needed for unique test"
      - &wallet_address
        name: wallet_address
        description: "Wallet address of sender or receiver. If amount is negative, wallet address is the sender's."
      - &token_address
        name: token_address
        description: "Contract address for token"
      - &evt_block_time
        name: evt_block_time
        description: "Timestamp for block event time in UTC"
      - &block_month
        name: block_month
        description: "Block time truncated to month used for partitioning"
      - &evt_tx_hash
        name: evt_tx_hash
        description: "Event transaction hash"
      - &evt_index
        name: evt_index
        description: "Event index"
      - &amount_raw
        name: amount_raw
        description: "Raw amount of ERC20 token held *before* taking into account token decimals"
      - &amount_decimal
        name: amount_decimal
        description: "Amount of ETH transferred in decimals from sender to recipient"

  - name: transfers_zksync_erc20
    meta:
      blockchain: zksync
      sector: transfers
      project: erc20
      contributors: soispoke, dot2dotseurat, tschubotz, Henrystats, lgingerich
    config:
      tags: ['transfers', 'zksync', 'erc20']
    description: "ERC20 Token Transfers on zksync."
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - transfer_type
            - wallet_address
            - evt_tx_hash
            - evt_index
    columns:
      - &blockchain
        name: blockchain
        description: "Blockchain"
      - &transfer_type
        name: transfer_type
        description: "Transfer type needed for unique test"
      - &wallet_address
        name: wallet_address
        description: "Wallet address of sender or receiver. If amount is negative, wallet address is the sender's."
      - &token_address
        name: token_address
        description: "Contract address for token"
      - &evt_block_time
        name: evt_block_time
        description: "Timestamp for block event time in UTC"
      - &block_month
        name: block_month
        description: "Block time truncated to month used for partitioning"
      - &evt_tx_hash
        name: evt_tx_hash
        description: "Event transaction hash"
      - &evt_index
        name: evt_index
        description: "Event index"
      - &amount_raw
        name: amount_raw
        description: "Raw amount of ERC20 token held *before* taking into account token decimals"

  - name: transfers_zksync_erc721
    meta:
      blockchain: zksync
      sector: transfers
      project: erc721
      contributors: lgingerich
    config:
      tags: ['transfers', 'zksync', 'erc721']
    description: "ERC721 Token Transfers on zksync."
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - transfer_type
            - wallet_address
            - evt_tx_hash
            - evt_index
    columns:
      - &blockchain
        name: blockchain
        description: "Blockchain"
      - &transfer_type
        name: transfer_type
        description: "Transfer type needed for unique test"
      - &wallet_address
        name: wallet_address
        description: "Wallet address of sender or receiver. If amount is negative, wallet address is the sender's."
      - &token_address
        name: token_address
        description: "Contract address for token"
      - &evt_block_time
        name: evt_block_time
        description: "Timestamp for block event time in UTC"
      - &block_month
        name: block_month
        description: "Block time truncated to month used for partitioning"
      - &evt_tx_hash
        name: evt_tx_hash
        description: "Event transaction hash"
      - &evt_index
        name: evt_index
        description: "Event index"
      - &tokenId
        name: tokenId
        description: "ID of the ERC721 token"
      - &amount
        name: amount
        description: "Amount of ERC721 token held"

  - name: transfers_zksync_erc1155
    meta:
      blockchain: zksync
      sector: transfers
      project: erc1155
      contributors: soispoke, dot2dotseurat, tschubotz, tomfutago, lgingerich
    config:
      tags: ['transfers', 'zksync', 'erc1155']
    description: "ERC1155 Token Transfers on zksync."
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - transfer_type
            - wallet_address
            - token_address
            - token_id
            - amount
    columns:
      - &blockchain
        name: blockchain
        description: "Blockchain"
      - &transfer_type
        name: transfer_type
        description: "Transfer Type: sent/received"
      - &wallet_address
        name: wallet_address
        description: "Wallet address of sender or receiver. If amount is negative, wallet address is the sender's."
      - &token_address
        name: token_address
        description: "Contract address for token"
      - &block_month
        name: block_month
        description: "Block Month column used to partition data in this table"
        tests:
          - not_null
      - &block_time
        name: block_time
        description: "Timestamp for block event time in UTC"
        tests:
          - not_null
      - &token_id
        name: token_id
        description: "ID of the ERC1155 token"
      - &amount
        name: amount
        description: "Amount of ERC1155 token held"
      - &tx_hash
        name: tx_hash
        description: "Transaction hash"
        tests:
          - not_null
