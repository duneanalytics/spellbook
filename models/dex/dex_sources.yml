version: 2

sources: 
  - name: optimism_legacy_ovm1
    description: >
      optimism legacy ovm1 tables
    tables:
      - name: logs
        loaded_at_field: block_time
      - name: creation_traces
        loaded_at_field: block_time
        