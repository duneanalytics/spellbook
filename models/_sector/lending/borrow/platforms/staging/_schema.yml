version: 2

models:
  - name: stg_aave_v1_ethereum_borrow
    meta:
      blockchain: ethereum
      sector: lending
      project: aave
      contributors: batwayne, chuxin, tomfutago
    config:
      tags: ['lending', 'borrow', 'aave', 'aave_v1', 'ethereum']
    description: "All Aave v1 transactions involving borrow transactions on Ethereum"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - transaction_type
            - token_address
            - evt_tx_hash
            - evt_index
    columns:
      - &blockchain
        name: blockchain
        description: "Blockchain name"
        tests:
          - not_null
      - &project
        name: project
        description: "Project name"
        tests:
          - not_null
      - &version
        name: version
        description: "Version of the contract built and deployed by the lending project"
        tests:
          - not_null
      - &transaction_type
        name: transaction_type
        description: "Transaction type"
        tests:
          - not_null
      - &token_address
        name: token_address
        description: "Token contract address"
        tests:
          - not_null
      - &borrower
        name: borrower
        description: "Borrower wallet address"
      - &repayer
        name: repayer
        description: "Repayer wallet address"
      - &liquidator
        name: liquidator
        description: "Liquidator wallet address"
      - &amount
        name: amount
        description: "Token amount present in the transaction"
      - &evt_tx_hash
        name: evt_tx_hash
        description: "Transaction hash of the event"
        tests:
          - not_null
      - &evt_index
        name: evt_index
        description: "Event index"
        tests:
          - not_null
      - &evt_block_month
        name: evt_block_month
        description: "Block Month column used to partition data in this table"
      - &evt_block_time
        name: evt_block_time
        description: "Timestamp for block event time in UTC"
        tests:
          - not_null
      - &evt_block_number
        name: evt_block_number
        description: "Event block number"
        tests:
          - not_null

  - name: stg_aave_v2_ethereum_borrow
    meta:
      blockchain: ethereum
      sector: lending
      project: aave
      contributors: batwayne, chuxin, tomfutago
    config:
      tags: ['lending', 'borrow', 'aave', 'aave_v2', 'ethereum']
    description: "All Aave v2 transactions involving borrow transactions on Ethereum"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - transaction_type
            - token_address
            - evt_tx_hash
            - evt_index
    columns:
      - *blockchain
      - *project
      - *version
      - *transaction_type
      - *token_address
      - *borrower
      - *repayer
      - *liquidator
      - *amount
      - *evt_tx_hash
      - *evt_index
      - *evt_block_month
      - *evt_block_time
      - *evt_block_number

  - name: stg_aave_v3_ethereum_borrow
    meta:
      blockchain: ethereum
      sector: lending
      project: aave
      contributors: batwayne, chuxin, tomfutago
    config:
      tags: ['lending', 'borrow', 'aave', 'aave_v3', 'ethereum']
    description: "All Aave v3 transactions involving borrow transactions on Ethereum"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - transaction_type
            - token_address
            - evt_tx_hash
            - evt_index
    columns:
      - *blockchain
      - *project
      - *version
      - *transaction_type
      - *token_address
      - *borrower
      - *repayer
      - *liquidator
      - *amount
      - *evt_tx_hash
      - *evt_index
      - *evt_block_month
      - *evt_block_time
      - *evt_block_number

  - name: stg_moola_v1_celo_borrow
    meta:
      blockchain: celo
      sector: lending
      project: moola
      contributors: tomfutago
    config:
      tags: ['lending', 'borrow', 'moola', 'aave_v2', 'celo']
    description: "All Moola transactions involving borrow transactions on Celo"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - transaction_type
            - token_address
            - evt_tx_hash
            - evt_index
    columns:
      - *blockchain
      - *project
      - *version
      - *transaction_type
      - *token_address
      - *borrower
      - *repayer
      - *liquidator
      - *amount
      - *evt_tx_hash
      - *evt_index
      - *evt_block_month
      - *evt_block_time
      - *evt_block_number

  - name: stg_aave_v2_polygon_borrow
    meta:
      blockchain: polygon
      sector: lending
      project: aave
      contributors: batwayne, chuxin, tomfutago
    config:
      tags: ['lending', 'borrow', 'aave', 'aave_v2', 'polygon']
    description: "All Aave v2 transactions involving borrow transactions on polygon"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - transaction_type
            - token_address
            - evt_tx_hash
            - evt_index
    columns:
      - *blockchain
      - *project
      - *version
      - *transaction_type
      - *token_address
      - *borrower
      - *repayer
      - *liquidator
      - *amount
      - *evt_tx_hash
      - *evt_index
      - *evt_block_month
      - *evt_block_time
      - *evt_block_number

  - name: stg_aave_v3_polygon_borrow
    meta:
      blockchain: polygon
      sector: lending
      project: aave
      contributors: batwayne, chuxin, tomfutago
    config:
      tags: ['lending', 'borrow', 'aave', 'aave_v3', 'polygon']
    description: "All Aave v3 transactions involving borrow transactions on polygon"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - transaction_type
            - token_address
            - evt_tx_hash
            - evt_index
    columns:
      - *blockchain
      - *project
      - *version
      - *transaction_type
      - *token_address
      - *borrower
      - *repayer
      - *liquidator
      - *amount
      - *evt_tx_hash
      - *evt_index
      - *evt_block_month
      - *evt_block_time
      - *evt_block_number

  - name: stg_aave_v3_optimism_borrow
    meta:
      blockchain: optimism
      sector: lending
      project: aave
      contributors: batwayne, chuxin, tomfutago
    config:
      tags: ['lending', 'borrow', 'aave', 'aave_v3', 'optimism']
    description: "All Aave v3 transactions involving borrow transactions on optimism"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - transaction_type
            - token_address
            - evt_tx_hash
            - evt_index
    columns:
      - *blockchain
      - *project
      - *version
      - *transaction_type
      - *token_address
      - *borrower
      - *repayer
      - *liquidator
      - *amount
      - *evt_tx_hash
      - *evt_index
      - *evt_block_month
      - *evt_block_time
      - *evt_block_number

  - name: stg_aave_v3_base_borrow
    meta:
      blockchain: base
      sector: lending
      project: aave
      contributors: batwayne, chuxin, tomfutago
    config:
      tags: ['lending', 'borrow', 'aave', 'aave_v3', 'base']
    description: "All Aave v3 transactions involving borrow transactions on base"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - transaction_type
            - token_address
            - evt_tx_hash
            - evt_index
    columns:
      - *blockchain
      - *project
      - *version
      - *transaction_type
      - *token_address
      - *borrower
      - *repayer
      - *liquidator
      - *amount
      - *evt_tx_hash
      - *evt_index
      - *evt_block_month
      - *evt_block_time
      - *evt_block_number

  - name: stg_aave_v3_arbitrum_borrow
    meta:
      blockchain: arbitrum
      sector: lending
      project: aave
      contributors: batwayne, chuxin, tomfutago
    config:
      tags: ['lending', 'borrow', 'aave', 'aave_v3', 'arbitrum']
    description: "All Aave v3 transactions involving borrow transactions on arbitrum"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - transaction_type
            - token_address
            - evt_tx_hash
            - evt_index
    columns:
      - *blockchain
      - *project
      - *version
      - *transaction_type
      - *token_address
      - *borrower
      - *repayer
      - *liquidator
      - *amount
      - *evt_tx_hash
      - *evt_index
      - *evt_block_month
      - *evt_block_time
      - *evt_block_number
