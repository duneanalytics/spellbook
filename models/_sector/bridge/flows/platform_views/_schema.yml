
version: 2
models:

 - name: base_ethereum_bridge_flows
   meta:
     blockchain: ["ethereum"]
     sector: bridge
     contributors: hildobby
   config:
     tags: ['bridge', 'flows', 'ethereum']
   description: "Bridge flows view for Base's native bridge"