
version: 2
models:

 - name: bridge_base_flows_view
   meta:
     blockchain: ["base"]
     sector: bridge
     contributors: hildobby
   config:
     tags: ['bridge', 'flows', 'base']
   description: "Bridge flows view for Base's official bridge"