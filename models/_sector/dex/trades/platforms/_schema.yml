version: 2

models:
  - name: uniswap_v3_arbitrum_base_trades
    meta:
      blockchain: arbitrum
      sector: dex
      project: uniswap
      contributors: jeff-dude, masquot, soispoke, mtitus6
    config:
      tags: [ 'arbitrum', 'dex', 'trades', 'uniswap', 'v3' ]
    description: "uniswap arbitrum v3 base trades"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - evt_index
      #- equal_rowcount:
         #compare_model: ref('uniswap_v3_arbitrum_trades')
      - equal_rowcount_with_sources:
          evt_sources:
            - source('uniswap_v3_arbitrum', 'Pair_evt_Swap')

  - name: uniswap_v3_base_base_trades
    meta:
      blockchain: base
      sector: dex
      project: uniswap
      contributors: wuligy
    config:
      tags: [ 'base', 'dex', 'trades', 'uniswap', 'v3' ]
    description: "uniswap base v3 base trades"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - evt_index
      - equal_rowcount:
          compare_model: ref('uniswap_v3_base_trades')
      #- equal_rowcount_with_sources:
         #evt_sources:
           #- source('uniswap_v3_base', 'UniswapV3Pool_evt_Swap')

  - name: uniswap_v3_bnb_base_trades
    meta:
      blockchain: bnb
      sector: dex
      project: uniswap
      contributors: chrispearcx
    config:
      tags: [ 'bnb', 'dex', 'trades', 'uniswap', 'v3' ]
    description: "uniswap bnb v3 base trades"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - evt_index
      - equal_rowcount:
          compare_model: ref('uniswap_v3_bnb_trades')
      - equal_rowcount_with_sources:
          evt_sources:
            - source('uniswap_v3_bnb', 'Pair_evt_Swap')

  - name: uniswap_v3_celo_base_trades
    meta:
      blockchain: celo
      sector: dex
      project: uniswap
      contributors: jeff-dude, masquot, soispoke, tomfutago
    config:
      tags: [ 'celo', 'dex', 'trades', 'uniswap', 'v3' ]
    description: "uniswap celo v3 base trades"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - evt_index
      - equal_rowcount:
          compare_model: ref('uniswap_v3_celo_trades')
      - equal_rowcount_with_sources:
          evt_sources:
            - source('uniswap_v3_celo', 'Pair_evt_Swap')

  - name: uniswap_v1_ethereum_base_trades
    meta:
      blockchain: ethereum
      sector: dex
      project: uniswap
      contributors: jeff-dude, masquot, soispoke, hosuke
    config:
      tags: [ 'ethereum', 'dex', 'trades', 'uniswap', 'v1']
    description: "uniswap ethereum v1 base trades"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - evt_index
      - equal_rowcount:
          compare_model: ref('uniswap_v1_ethereum_trades')
      - equal_rowcount_with_sources:
          evt_sources:
            - source('uniswap_ethereum', 'Exchange_evt_TokenPurchase')
            - source('uniswap_ethereum', 'Exchange_evt_EthPurchase')
      - check_seed:
          seed_file: ref('uniswap_ethereum_base_trades_seed')
          filter:
            blockchain: ethereum
            project: uniswap
            version: 1
          match_columns:
            - tx_hash
            - evt_index
          check_columns:
            - token_bought_address
            - token_sold_address
            - token_bought_amount_raw
            - token_sold_amount_raw

  - name: uniswap_v2_ethereum_base_trades
    meta:
      blockchain: ethereum
      sector: dex
      project: uniswap
      contributors: jeff-dude, masquot, soispoke, hosuke
    config:
      tags: [ 'ethereum', 'dex', 'trades', 'uniswap', 'v2' ]
    description: "uniswap ethereum v2 base trades"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - evt_index
      #- equal_rowcount:
        #compare_model: ref('uniswap_v2_ethereum_trades')
      #As there are negative filters, there will be fewer rows
      #- equal_rowcount_with_sources:
        #evt_sources:
          #- source('uniswap_v2_ethereum', 'Pair_evt_Swap')
      - check_seed:
          seed_file: ref('uniswap_ethereum_base_trades_seed')
          filter:
            blockchain: ethereum
            project: uniswap
            version: 2
          match_columns:
            - tx_hash
            - evt_index
          check_columns:
            - token_bought_address
            - token_sold_address
            - token_bought_amount_raw
            - token_sold_amount_raw

  - name: uniswap_v3_ethereum_base_trades
    meta:
      blockchain: ethereum
      sector: dex
      project: uniswap
      contributors: jeff-dude, masquot, soispoke, hosuke
    config:
      tags: [ 'ethereum', 'dex', 'trades', 'uniswap', 'v3' ]
    description: "uniswap ethereum v3 base trades"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - evt_index
      - equal_rowcount:
          compare_model: ref('uniswap_v3_ethereum_trades')
      - equal_rowcount_with_sources:
          evt_sources:
            - source('uniswap_v3_ethereum', 'Pair_evt_Swap')
      - check_seed:
          seed_file: ref('uniswap_ethereum_base_trades_seed')
          filter:
            blockchain: ethereum
            project: uniswap
            version: 3
          match_columns:
            - tx_hash
            - evt_index
          check_columns:
            - token_bought_address
            - token_sold_address
            - token_bought_amount_raw
            - token_sold_amount_raw

  - name: uniswap_v3_optimism_base_trades
    meta:
      blockchain: optimism
      sector: dex
      project: uniswap
      contributors: 0xBoxer, jeff-dude, hosuke
    config:
      tags: [ 'optimism', 'dex', 'trades', 'uniswap', 'v3' ]
    description: "uniswap optimism v3 base trades"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - evt_index
      - equal_rowcount:
          compare_model: ref('uniswap_v3_optimism_trades')
      - equal_rowcount_with_sources:
          evt_sources:
            - source('uniswap_v3_optimism', 'Pair_evt_Swap')
            
  - name: uniswap_v3_polygon_base_trades
    meta:
      blockchain: polygon
      sector: dex
      project: uniswap
      contributors: Henrystats, hosuke
    config:
      tags: [ 'polygon', 'dex', 'trades', 'uniswap', 'v3' ]
    description: "uniswap polygon v3 base trades"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - evt_index
      #- equal_rowcount:
        #compare_model: ref('uniswap_v3_polygon_trades')
      - equal_rowcount_with_sources:
          evt_sources:
            - source('uniswap_v3_polygon', 'UniswapV3Pool_evt_Swap')

  - name: uniswap_trades_beta
    meta:
      blockchain: ethereum, optimism, polygon, arbitrum, bnb, base, celo
      sector: dex
      project: uniswap
      contributors: jeff-dude, masquot, soispoke, 0xBoxer, Henrystats, chrispearcx, wuligy, fomfutago, hosuke
    config:
      tags: [ 'ethereum', 'optimism', 'polygon', 'uniswap','trades', 'dex', 'jeff-dude','cross-chain', 'bnb', 'base', 'celo' ]
    description: >
      Uniswap trades on all chains across all contracts and versions. This table will load dex trades downstream.

  - name: defiswap_ethereum_base_trades
    meta:
      blockchain: ethereum
      sector: dex
      project: defiswap
      contributors: pandajackson42, 0xRob, hosuke
    config:
      tags: [ 'ethereum', 'dex', 'trades', 'defiswap' ]
    description: "Defiswap ethereum base trades"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - evt_index
      - equal_rowcount:
          compare_model: ref('defiswap_ethereum_trades')
      - equal_rowcount_with_sources:
          evt_sources:
            - source('defiswap_ethereum', 'CroDefiSwapPair_evt_Swap')
      - check_seed:
          seed_file: ref('defiswap_ethereum_base_trades_seed')
          match_columns:
            - tx_hash
            - evt_index
          check_columns:
            - token_bought_address
            - token_sold_address
            - token_bought_amount_raw
            - token_sold_amount_raw

  - name: defiswap_trades_beta
    meta:
      blockchain: ethereum
      sector: dex
      project: defiswap
      contributors: pandajackson42, hosuke
    config:
      tags: [ 'ethereum','defiswap','trades', 'dex' ]
    description: >
      defiswap trades