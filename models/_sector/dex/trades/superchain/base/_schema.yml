version: 2

models:
  - name: dex_superchain_base_base_trades

  - name: baseswap_base_base_trades
    meta:
      blockchain: base
      sector: dex
      project: baseswap
      contributors: ARDev097
    config:
      tags: [ 'base', 'dex', 'trades', 'baseswap' ]
    description: "baseswap base tradescon base"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - evt_index
      - check_dex_base_trades_seed:
          seed_file: ref('baseswap_base_base_trades_seed')

