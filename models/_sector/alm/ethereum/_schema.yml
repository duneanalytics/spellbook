version: 2

models:
  - name: alm_ethereum_base_trades
    tests:
      - check_dex_info_relationship

  - name: arrakis_v2_ethereum_base_trades
    meta:
      blockchain: ethereum
      sector: alm
      project: arrakis
      contributors: 0xrusowsky
    config:
      tags: [ 'ethereum', 'alm', 'arrakis', 'dex', 'trades' ]
    description: "arrakis finance ethereum v2 base trades"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - evt_index
            - vault_address
      # - check_dex_base_lps_seed:
      #     seed_file: ref('uniswap_ethereum_base_lps_seed')
      #     filter:
      #       version: 3