version: 2

models:
  - name: arrakis_finance_ethereum_base_trades
    meta:
      blockchain: ethereum
      sector: alm
      project: arrakis_finance
      contributors: 0xrusowsky
    config:
      tags: [ 'ethereum', 'alm', 'arrakis', 'arrakis_finance', 'trades' ]
    description: "arrakis finance ethereum base trades"
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - evt_index
            - vault_address
      # - check_dex_base_lps_seed:
      #     seed_file: ref('uniswap_ethereum_base_lps_seed')
      #     filter:
      #       version: 3