version: 2

models:
  - name: fungible_arbitrum_transfers
    meta:
      blockchain: arbitrum
      sector: fungible
      contributors: hildobby
    config:
      tags: ['fungible', 'arbitrum', 'transfers']
    description: >
        Native and ERC20 transfers
    columns:
      - &blockchain
        name: blockchain
        description: "Blockchain"
      - &block_time
        name: block_time
        description: "UTC event block time"
      - &block_number
        name: block_number
        description: "Block number in which the transaction was executed"
      - &block_month
        name: block_month
        description: "UTC event block month"
      - &amount_raw
        name: amount_raw
        description: "Raw amount"
      - &amount
        name: amount
        description: "Amount"
      - &usd_price
        name: usd_price
        description: "Price in USD"
      - &usd_amount
        name: usd_amount
        description: "Amount in USD"
      - &contract_address
        name: contract_address
        description: "Fungible contract address"
      - &symbol
        name: symbol
        description: "Symbol of fungible token"
      - &decimals
        name: decimals
        description: "The number of decimals of the fungible token"
      - &token_standard
        name: token_standard
        description: "Token standard"
        tests:
          - accepted_values:
              values: ['native', 'erc20']
      - &tx_from
        name: tx_from
        description: "Address that initiated the transaction"
      - &from
        name: from
        description: "Address that sent the token(s)"
      - &to
        name: to
        description: "Address that received the token(s)"
      - &tx_hash
        name: tx_hash
        description: "Transaction hash"
      - &evt_index
        name: evt_index
        description: "Event index"

  - name: fungible_avalanche_c_transfers
    meta:
      blockchain: avalanche_c
      sector: fungible
      contributors: hildobby
    config:
      tags: ['fungible', 'avalanche_c', 'transfers']
    description: >
        Native and ERC20 transfers
    columns:
      - *blockchain
      - *block_time
      - *block_number
      - *block_month
      - *amount_raw
      - *amount
      - *usd_price
      - *usd_amount
      - *contract_address
      - *symbol
      - *decimals
      - *token_standard
      - *tx_from
      - *from
      - *to
      - *tx_hash
      - *evt_index

  - name: fungible_bnb_transfers
    meta:
      blockchain: bnb
      sector: fungible
      contributors: hildobby
    config:
      tags: ['fungible', 'bnb', 'transfers']
    description: >
        Native and ERC20 transfers
    columns:
      - *blockchain
      - *block_time
      - *block_number
      - *block_month
      - *amount_raw
      - *amount
      - *usd_price
      - *usd_amount
      - *contract_address
      - *symbol
      - *decimals
      - name: token_standard
        description: "Token standard"
        tests:
          - accepted_values:
              values: ['native', 'bep20']
      - *tx_from
      - *from
      - *to
      - *tx_hash
      - *evt_index

  - name: fungible_ethereum_transfers
    meta:
      blockchain: ethereum
      sector: fungible
      contributors: hildobby
    config:
      tags: ['fungible', 'ethereum', 'transfers']
    description: >
        Native and ERC20 transfers
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tx_hash
            - evt_index
            - from
            - to
    columns:
      - *blockchain
      - *block_time
      - *block_number
      - *block_month
      - *amount_raw
      - *amount
      - *usd_price
      - *usd_amount
      - *contract_address
      - *symbol
      - *decimals
      - *token_standard
      - *tx_from
      - *from
      - *to
      - *tx_hash
      - *evt_index

  - name: fungible_fantom_transfers
    meta:
      blockchain: fantom
      sector: fungible
      contributors: hildobby
    config:
      tags: ['fungible', 'fantom', 'transfers']
    description: >
        Native and ERC20 transfers
    columns:
      - *blockchain
      - *block_time
      - *block_number
      - *block_month
      - *amount_raw
      - *amount
      - *usd_price
      - *usd_amount
      - *contract_address
      - *symbol
      - *decimals
      - *token_standard
      - *tx_from
      - *from
      - *to
      - *tx_hash
      - *evt_index

  - name: fungible_gnosis_transfers
    meta:
      blockchain: gnosis
      sector: fungible
      contributors: hildobby
    config:
      tags: ['fungible', 'gnosis', 'transfers']
    description: >
        Native and ERC20 transfers
    columns:
      - *blockchain
      - *block_time
      - *block_number
      - *block_month
      - *amount_raw
      - *amount
      - *usd_price
      - *usd_amount
      - *contract_address
      - *symbol
      - *decimals
      - *token_standard
      - *tx_from
      - *from
      - *to
      - *tx_hash
      - *evt_index

  - name: fungible_optimism_transfers
    meta:
      blockchain: optimism
      sector: fungible
      contributors: hildobby
    config:
      tags: ['fungible', 'optimism', 'transfers']
    description: >
        Native and ERC20 transfers
    columns:
      - *blockchain
      - *block_time
      - *block_number
      - *block_month
      - *amount_raw
      - *amount
      - *usd_price
      - *usd_amount
      - *contract_address
      - *symbol
      - *decimals
      - *token_standard
      - *tx_from
      - *from
      - *to
      - *tx_hash
      - *evt_index

  - name: fungible_polygon_transfers
    meta:
      blockchain: polygon
      sector: fungible
      contributors: hildobby
    config:
      tags: ['fungible', 'polygon', 'transfers']
    description: >
        Native and ERC20 transfers
    columns:
      - *blockchain
      - *block_time
      - *block_number
      - *block_month
      - *amount_raw
      - *amount
      - *usd_price
      - *usd_amount
      - *contract_address
      - *symbol
      - *decimals
      - *token_standard
      - *tx_from
      - *from
      - *to
      - *tx_hash
      - *evt_index

  - name: fungible_celo_transfers
    meta:
      blockchain: celo
      sector: fungible
      contributors: hildobby
    config:
      tags: ['fungible', 'celo', 'transfers']
    description: >
        Native and ERC20 transfers
    columns:
      - *blockchain
      - *block_time
      - *block_number
      - *block_month
      - *amount_raw
      - *amount
      - *usd_price
      - *usd_amount
      - *contract_address
      - *symbol
      - *decimals
      - *token_standard
      - *tx_from
      - *from
      - *to
      - *tx_hash
      - *evt_index
