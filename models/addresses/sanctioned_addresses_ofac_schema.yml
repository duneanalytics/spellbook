version: 2

models:
  - name: sanctioned_addresses_ofac
    meta:
      sector: ofac
      project: addresses
      contibutors: hildobby
    config:
      tags: ['table', 'ofac', 'sanctionned', 'addresses', 'ethereum']
    description: "Known list of OFAC sanctioned addresses"
    columns:
      - name: address
        description: "OFAC sanctioned address"
      - name: protocol
        description: "Name of protocol behind this OFAC sanctioned address"
      - name: description
        description: "Address description"
      - name: blockchain
        description: "Blockchain on which this address has been sanctioned by OFAC"
      - name: currency_contract
        description: "Contract address of OFAC sanctionned currency"
      - name: currency_name
        description: "Currency symbol of OFAC sanctionned currency"
