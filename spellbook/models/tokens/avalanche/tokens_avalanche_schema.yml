version: 2

models:
  - name: tokens_avalanche_erc20
    meta:
      blockchain: avalanche
      sector: tokens
      project: erc20
      contibutors: 0xManny
    config:
      tags: ['table', 'erc20', 'avalanche']
    description: "ERC20 Token Addresses, Symbols and Decimals"
    columns:
      - name: contract_address
        description: "ERC20 token contract address on avalanche"
      - name: symbol
        description: "ERC20 token symbol"
      - name: decimals
        description: "Number of decimals, refers to how divisible an ERC20 token can be"

  - name: tokens_avalanche_nft
    meta:
      blockchain: avalanche
      sector: nft
      contributors: 0xManny
    config:
      tags: ['tokens', 'avalanche', 'nft', 'erc721', 'erc1155']
    description: >
        Selection of NFT token addresses.
    columns:
      - name: contract_address
        description: "The contract address is the unique address of where the NFTs are stored on avalanche."
      - name: name
        description: "NFT project name"
      - name: symbol
        description: "NFT project symbol"
